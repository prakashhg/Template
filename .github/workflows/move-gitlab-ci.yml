
name: Test Workflow

on:
  workflow_dispatch:    # Allows manual trigger from Actions tab
  push:                 # Also runs on any push

jobs:
  hello-world:
    runs-on: ubuntu-latest
       steps:
      - name: Print greeting
